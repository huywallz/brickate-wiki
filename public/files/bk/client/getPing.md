Gets the current round-trip time (RTT) between the client and the server.

```
bk.client.getPing()
```

## Parameters
None.

## Returns
**<code>number ping</code>**
> The network latency measured in milliseconds (ms).